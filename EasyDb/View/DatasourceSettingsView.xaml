<mah:MetroWindow x:Class="EasyDb.View.DatasourceSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:items="clr-namespace:EasyDb.ViewModel.DataSource.Items"
        xmlns:customControls="clr-namespace:EasyDb.CustomControls"
        xmlns:converters="clr-namespace:EasyDb.Converters"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:viewModel="clr-namespace:EasyDb.ViewModel"
        mc:Ignorable="d"
        Title="{StaticResource dsms_settingsWindowTitle}" d:DataContext="{d:DesignInstance items:UserDataSource}" Height="450" Width="800" ShowMinButton="False" ShowMaxRestoreButton="False" ShowIconOnTitleBar="False">
    <mah:MetroWindow.Resources>
        <converters:EmptyConverter x:Key="EmptyConverter"/>
    </mah:MetroWindow.Resources>
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <i:Interaction.Behaviors>
            <viewModel:ValidationExceptionBehavior />
        </i:Interaction.Behaviors>
        <Grid.Resources>
            <ResourceDictionary>
                <Style TargetType="MenuItem"
                       BasedOn="{StaticResource StandardMenuItem}" />
                <Style TargetType="Menu"
                       BasedOn="{StaticResource StandardMenu}" />
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".25*"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0">
            </DockPanel>
            <ListBox Grid.Row="1"></ListBox>
        </Grid>
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="{DynamicResource ControlBorderBrush}"></GridSplitter>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="65"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition MinHeight="45" Height="*"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="5">
                <DockPanel Margin="0,0,0,4" LastChildFill="True">
                    <TextBlock MinWidth="100" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{StaticResource dsms_datasourceName}"></TextBlock>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True, NotifyOnValidationError=True}"></TextBox>
                </DockPanel>
                <DockPanel>
                    <TextBlock MinWidth="100" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{StaticResource dsms_datasourceComment}"></TextBlock>
                    <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                </DockPanel>
            </StackPanel>
            <ScrollViewer Grid.Row="1" Background="{StaticResource GrayBrush10}">
                <mah:MetroAnimatedTabControl 
                    BorderThickness="1"
                    BorderBrush="{DynamicResource AccentColorBrush3}"
                    Background="{StaticResource GrayBrush10}"
                    mah:TabControlHelper.Underlined="TabPanel"
                    VerticalAlignment="Stretch" 
                    HorizontalAlignment="Stretch"
                    SelectedValue="{Binding SelectedDataSourceOption}"
                    ItemsSource="{Binding SettingsObjects}">
                    <TabControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontSize="14" FontWeight="Bold" Text="{Binding OptionsDefinitionName}"/>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                    <TabControl.ContentTemplate>
                        <DataTemplate>
                            <customControls:UserDatasouceSettingsControl 
                                SelectedObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type mah:MetroWindow}}, Path=DataContext.SelectedDataSourceOption, Converter={StaticResource EmptyConverter}}">
                            </customControls:UserDatasouceSettingsControl>
                        </DataTemplate>
                    </TabControl.ContentTemplate>
                </mah:MetroAnimatedTabControl>
            </ScrollViewer>
            <StackPanel Margin="10" HorizontalAlignment="Left" Grid.Row="2">
                <DockPanel>
                    <Button MinWidth="100" Content="$testConn"></Button>
                    <TextBlock Margin="10,0" Text="Not tested yet" VerticalAlignment="Center" FontStyle="Italic"></TextBlock>
                </DockPanel>

                <CheckBox Margin="0,5" Content="$savepassword"/>
            </StackPanel>
            <DockPanel Margin="5" HorizontalAlignment="Right" Grid.Row="3">
                <Button Command="" Content="$apply_name" Style="{StaticResource MetroButtonAccentNoBorder}" Margin="0,2,10,2"  Width="120"/>
                <Button Content="$cancel_name" Style="{StaticResource MetroButton}" Width="120" />
            </DockPanel>
        </Grid>
    </Grid>
</mah:MetroWindow>
